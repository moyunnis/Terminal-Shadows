# Единые окончания строк независимо от ОС разработчика
* text=auto eol=lf

# Скрипты Unix — всегда LF (иначе не запустятся на Linux)
*.sh text eol=lf

# Пакетные файлы Windows — всегда CRLF
*.bat text eol=crlf

# Бинарные/данные — не трогать
*.dat binary
